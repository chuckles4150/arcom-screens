# Copy this to .env and fill in real values. .env is gitignored.

# The password Chuck (and anyone else with access) will use to log in
# to screens.arcom.site. Change this to something strong.
DASHBOARD_PASSWORD=changeme
