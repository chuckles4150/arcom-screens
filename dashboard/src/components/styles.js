// Deprecated: kept as a re-export shim during the redesign.
// The token map moved to dashboard/src/theme.js with full names
// (arcNavy, arcSage700, fgBrand, …) instead of the old abbreviated ones.
// Safe to delete once you confirm nothing imports `./styles.js`.
export { T } from '../theme.js';
